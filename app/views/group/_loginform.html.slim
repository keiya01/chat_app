.new-form
    h3 グループに入ろう！
    p.login-text 問題に正解するとグループに入ることが出来ます！
    .error
      - if flash[:error]
        p = flash[:error]
      - @user.errors.full_messages.each do |message|
        p = message
    = form_tag "/chatroom/#{@group.entry_pass}/login_form" do
      label name="name_id" = @group.question
      br
      input.input-text type="text" name="user_answer" placeholder="答えを入力" value="#{params[:user_answer]}"
      br
      input.submit-btn type="submit" value='送信' data-disable-with="送信中..."